class Handler {
    (Int) -> String formatter
    constructor(f: (Int) -> String) { formatter = f }
    format(n: Int): String { return formatter(n) }
}
main() {
    var h: Handler = Handler((n: Int) -> { return "value=" + n })
    println(h.format(42))
}
