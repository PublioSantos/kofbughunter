main() {
    var x = 2
    var label = switch (x) {
        case 1 -> "one"
        case 2 -> "two"
        default -> "other"
    }
    println(label)
    var y = switch (x * 2) {
        case 4 -> "four"
        default -> "?"
    }
    println(y)
}
