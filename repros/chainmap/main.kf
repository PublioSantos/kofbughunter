main() {
    var nums: List<Int> = listOf()
    nums.add(1)
    nums.add(2)
    nums.add(3)
    var doubled: List<Int> = nums.map((x: Int) -> { return x * 2 })
    for (var n in doubled) { println(n) }
}
